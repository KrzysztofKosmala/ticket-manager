spring:
  ai:
    mcp:
      client:
        enabled: false
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: test-model
          temperature: 0.0
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8085/realms/test

eureka:
  client:
    enabled: false

openapi:
  service:
    url: http://localhost:8099
