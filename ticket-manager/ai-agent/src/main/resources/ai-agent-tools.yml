ai-agent:
  tools:
    enforce-scopes: false
    registry:
      "[tm.orders.search]":
        enabled: true
        source: INTERNAL_MCP
        access-mode: READ
        required-scopes:
          - tools:orders.read
